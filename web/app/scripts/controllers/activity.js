'use strict';

/**
 * @ngdoc function
 * @name pmtoolApp.controller:ActivityCtrl
 * @description
 * # ActivityCtrl
 * Controller of the pmtoolApp
 */
angular.module('pmtoolApp')
  .controller('ActivityCtrl', function ($scope, $rootScope, UserService, $cookieStore) {
    
  });
